'use client';

import { AddBuilding } from '@/features/facilities';

/**
 * Trang thêm tòa nhà mới dành cho Manager.
 * Đường dẫn: /dashboard/manager/facilities/new
 */
export default function NewBuildingPage() {
  return <AddBuilding />;
}
