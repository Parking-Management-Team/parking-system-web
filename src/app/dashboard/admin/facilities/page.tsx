'use client';

import { BuildingDirectory } from '@/features/facilities';

/**
 * Trang quản lý cơ sở vật chất (Facilities Management) dành cho Admin.
 * Sử dụng chung component và logic của phân hệ facilities.
 */
export default function AdminFacilityManagementPage() {
  return <BuildingDirectory />;
}
