'use client';

import { FloorManagement } from '@/features/facilities';

/**
 * Trang cấu hình Tầng (Floor & Zone Management) dành cho Admin.
 */
export default function AdminBuildingFloorsPage() {
  return <FloorManagement />;
}
