'use client';

import React from 'react';
import { ShiftReportPlaceholder } from '@/features/staff';

/**
 * Shift Reports Page - Excluded from current release scope (FUT-SHIFT-001).
 */
export default function ShiftReportsPage() {
  return <ShiftReportPlaceholder />;
}
