'use client';

import { SubscriptionWorkspace } from '@/features/subscriptions';

/**
 * Monthly Subscription Management Page for Manager.
 * Uses Feature-Based architecture, with logic and UI encapsulated inside `@/features/subscriptions`.
 */
export default function MonthlySubscriptionsPage() {
  return <SubscriptionWorkspace />;
}
