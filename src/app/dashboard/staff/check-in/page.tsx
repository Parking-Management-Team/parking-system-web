'use client';

import React from 'react';
import { VehicleCheckin } from '@/features/vehicles';

/**
 * Vehicle Check-in Page - registers and admits vehicles.
 * Renders the Vehicle Check-in Portal from features/vehicles.
 */
export default function VehicleCheckinPage() {
  return <VehicleCheckin />;
}
