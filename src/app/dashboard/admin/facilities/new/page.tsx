'use client';

import { AddBuilding } from '@/features/facilities';

/**
 * Trang thêm tòa nhà mới dành cho Admin.
 * Đường dẫn: /dashboard/admin/facilities/new
 */
export default function AdminNewBuildingPage() {
  return <AddBuilding />;
}
