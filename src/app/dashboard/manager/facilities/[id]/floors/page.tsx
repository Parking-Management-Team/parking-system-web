'use client';

import React from 'react';
import { FloorManagement } from '@/features/facilities';

/**
 * Trang cấu hình Tầng (Floor & Zone Management) dưới dạng Sub-route của Tòa nhà
 */
export default function BuildingFloorsPage() {
  return <FloorManagement />;
}
