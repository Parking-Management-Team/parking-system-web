'use client';

import React from 'react';
import { AuditLogsWorkspace } from '@/features/admin';

/**
 * System Audit Logs Page (Admin Portal)
 * Renders the high-fidelity audit trail manager for system actions.
 */
export default function AuditLogsPage() {
  return <AuditLogsWorkspace />;
}
