'use client';

import { BuildingDetails } from '@/features/facilities';

/**
 * Trang chi tiết tòa nhà (cấu hình chung) dành cho Admin.
 * Đường dẫn: /dashboard/admin/facilities/[id]
 */
export default function AdminBuildingDetailsPage() {
  return <BuildingDetails />;
}
